import { PositionType } from "../types/position.type";
import { SizeType } from "../types/size.type";

export type WrapperConfig = SizeType & PositionType

export interface IWrapper {
    // setParams: () => void
}